# Docker運用向け（本番/実行）依存
openai>=1.40.0
typer>=0.12.3
rich>=13.7.1
pyyaml>=6.0.2
watchdog>=4.0.1
discord.py>=2.4.0

# 実運用で必要になることが多い（README/Makeで使用）
ruff>=0.6.0
pytest>=8.0.0
requests>=2.32.0
textual>=0.58.0
